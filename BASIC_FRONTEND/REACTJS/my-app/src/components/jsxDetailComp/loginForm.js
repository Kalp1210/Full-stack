import React from "react";

const LoginForm = () => {
  return (
    <div>
      loginForm
      <input type="text" />
      <input type="password" />
      <button>Submit</button>
    </div>
  );
};

export default LoginForm;

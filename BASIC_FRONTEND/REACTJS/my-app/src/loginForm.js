import React from "react";
// we can only return one high lvl tag

export default function loginForm() {
  return (
    <form>
      <input type="passsword" />
      <input type="text" />
      <button>Submit!!</button>
    </form>
  );
}
